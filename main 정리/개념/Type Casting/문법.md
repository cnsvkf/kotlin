
- as? : 타입 변환, 실패 시 null 
    
    - 형식

```kotlin
fun main() {
    val value: Any = "hello"
    val result: Int? = value as? Int
    println(result)
} // null
```

- as : 강제 타입 변환

    - 형식

```kotlin
val value: Any = "Hello"

val text: String = value as String
```

- as! : 스위프트 문법, 코틀린에 없음