> ## 오버로딩 
- 같은 이름이여도 매개변수가 다르면 다른 함수
    - 형식
```kotlin
fun printValue(num: Int) {
    println(num)
}

fun printValue(text: String) {
    println(text)
}

fun main() {
    printValue(10)
    printValue("hello")
}
```